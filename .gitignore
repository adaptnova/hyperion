# Large Artifacts & Logs
checkpoints/*.safetensors
checkpoints/*.pt
# data/* # Keep data definition files, ignore large binaries if added later
logs/
artifacts/soul_vector_v1.pt # Keep initial soul vector definition? Maybe track pointers later.

# Python Caches & Environment
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/

# Secrets (Should be managed separately)
config/*key*
config/*.env
# Example: WandB key if used
wandb/

# OS & Editor files
.DS_Store
*.swp

# Cloned Dependency Repos (Code is pulled dynamically or installed)
Qwen-VL/
Qwen-Agent/ # If cloned instead of installed via pip

# Temporary files from scripts
/tmp/*_tools.json # From runner script logging

.env
